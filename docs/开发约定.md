# 开发约定

## 分支与提交
- 在功能分支开发。
- 提交信息统一使用中文。

## 代码约定
- 后端：分层结构（controller/service/repository/domain）。
- 前端：按页面与api模块组织。
- 统一错误响应结构。

## 运行约定
- 使用 docker-compose 启动本地 PostgreSQL。
- 后端通过 Spring Profile `dev` 连接本地数据库。
